DASHBOARD_PASSWORD=replace-with-a-strong-password
PROSTAR_ENROLLMENT_SECRET=generate-at-least-32-random-characters
# Required outside local development so enrolled Prostar sessions survive restarts.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Optional. A random Prostar viewer password is generated for each install otherwise.
# PROSTAR_BOOTSTRAP_VIEWER_PASSWORD=
